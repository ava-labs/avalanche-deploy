{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockscout.fullname" . }}-redis
  labels:
    {{- include "blockscout.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.redis.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "blockscout.redisSelectorLabels" . | nindent 4 }}
{{- end }}
