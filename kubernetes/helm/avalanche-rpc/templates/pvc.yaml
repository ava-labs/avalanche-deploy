{{- if .Values.l1_rpc_persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "l1-rpc.fullname" . }}
  labels:
    {{- include "l1-rpc.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.l1_rpc_persistence.accessModes | nindent 4 }}
  {{- if .Values.l1_rpc_persistence.storageClass }}
  storageClassName: {{ .Values.l1_rpc_persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.l1_rpc_persistence.size }}
{{- end }}
