{{- if .Values.l1_validator_serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "l1-validator.serviceAccountName" . }}
  labels:
    {{- include "l1-validator.labels" . | nindent 4 }}
{{- end }}
