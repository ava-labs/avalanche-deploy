{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "avalanche-validator.serviceAccountName" . }}
  labels:
    {{- include "avalanche-validator.labels" . | nindent 4 }}
{{- end }}
