{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "safe.fullname" . }}-redis
  labels:
    {{- include "safe.redis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "safe.redis.selectorLabels" . | nindent 4 }}
{{- end }}
