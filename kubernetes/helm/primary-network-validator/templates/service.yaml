apiVersion: v1
kind: Service
metadata:
  name: {{ include "primary-network-validator.fullname" . }}
  labels:
    {{- include "primary-network-validator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.primary_validator_service.type }}
  clusterIP: None  # Headless service for StatefulSet
  ports:
    - port: {{ .Values.primary_validator_service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.primary_validator_service.stakingPort }}
      targetPort: staking
      protocol: TCP
      name: staking
  selector:
    {{- include "primary-network-validator.selectorLabels" . | nindent 4 }}
---
# LoadBalancer for P2P connectivity
apiVersion: v1
kind: Service
metadata:
  name: {{ include "primary-network-validator.fullname" . }}-p2p
  labels:
    {{- include "primary-network-validator.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  ports:
    - port: {{ .Values.primary_validator_service.stakingPort }}
      targetPort: staking
      protocol: TCP
      name: staking
  selector:
    {{- include "primary-network-validator.selectorLabels" . | nindent 4 }}
