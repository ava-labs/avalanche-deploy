[defaults]
inventory = inventory/
roles_path = roles/
host_key_checking = False
timeout = 30
forks = 10

[ssh_connection]
pipelining = False
ssh_args = -o ControlMaster=auto -o ControlPersist=30m -o ServerAliveInterval=30 -o ServerAliveCountMax=20
