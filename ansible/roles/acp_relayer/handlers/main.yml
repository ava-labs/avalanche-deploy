---
- name: Restart relayerd
  ansible.builtin.systemd:
    name: relayerd
    state: restarted
    daemon_reload: true

- name: Restart relayer console
  ansible.builtin.systemd:
    name: relayer-console
    state: restarted
    daemon_reload: true
